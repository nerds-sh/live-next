export default function Home() {
  // What is CORS?
  // Let's say we can't call https://jsonplaceholder.typicode.com/photos because of CORS
  // create API which gets the photos from https://jsonplaceholder.typicode.com/photos by
  //
  // create 3 sections in the homepage for each album from https://jsonplaceholder.typicode.com/photos
  // each section should be linked to a dedicated page
  // create a dynamic page (/album/albumId) which displays the images from the corresponding album

  return <div>display the sections here</div>;
}
